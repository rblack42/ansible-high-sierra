---
- name: install macvim
  homebrew_cask:
    name=macvim
    state=present

- name: install Vundle
  git:
      repo: https://github.com/VundleVim/Vundle.Vim.git
      dest: ~/.vim/bundle/Vyndle.vim

- name: install editorconfig
  homebrew:
    name: editorconfig
    state: present
